diff --git a/evidyaloka/settings.py b/evidyaloka/settings.py
index 792c0c9..e5eaa0d 100644
--- a/evidyaloka/settings.py
+++ b/evidyaloka/settings.py
@@ -1,6 +1,6 @@
 # # Django settings for evd project.
 import os
-from config_prod import *
+from config_dev import *
 from dotenv import load_dotenv
 from pathlib import Path
 PROJECT_DIR = os.path.abspath(os.path.dirname(__file__))
@@ -83,14 +83,14 @@ DATABASES = {
     'default': {
         'ENGINE': 'django.db.backends.mysql',
 
-        'NAME': 'evldb',
+        'NAME': 'evd_replica',
 
         'USER': 'root',
-        'PASSWORD': 'Dwiti@1995',
-        'HOST': '',
+        'PASSWORD': 'evadmin123',
+        'HOST': os.environ.get('EVD_DB_HOST', ''),   # '' = local socket (native dev); 'db' in Docker
         'PORT': '',
         'OPTIONS': {
-        'init_command': "SET default_storage_engine=MYISAM; \
+        'init_command': "SET default_storage_engine=INNODB; \
                      SET NAMES 'utf8mb4'; \
                      SET CHARACTER SET utf8mb4; \
                      SET character_set_connection=utf8mb4;",
@@ -105,7 +105,7 @@ DATABASES = {
         'HOST': '',  # Specify the hostname where your MySQL server is running.
         'PORT': '',       # Specify the port number for your MySQL server.
         'OPTIONS': {
-            "init_command": "SET default_storage_engine=MYISAM",
+            "init_command": "SET default_storage_engine=INNODB",
         }
     }
 }
@@ -150,6 +150,9 @@ TEMPLATES = [
                 'django.template.context_processors.request',
                 'django.contrib.auth.context_processors.auth',
                 'django.contrib.messages.context_processors.messages',
+                'vrm_services.context_processor.add_role_based_tabs',
+                'vrm_services.context_processor.add_role_based_flags',
+                'vrm_services.context_processor.serve_context_processor',
                 # Add any other context processors you might need
             ],
         },
@@ -225,6 +228,8 @@ INSTALLED_APPS = (
     'questionbank',
     'genutilities',
     'student',
+    'vrm_services',
+    
     'corsheaders',
 #    'cronjobs',
     #'django_cron'
@@ -245,6 +250,9 @@ TEMPLATES = [
                 'django.contrib.messages.context_processors.messages',
                 'django.template.context_processors.media',   # For media files
                 'django.template.context_processors.static',  # For static files
+                'vrm_services.context_processor.add_role_based_tabs',
+                'vrm_services.context_processor.add_role_based_flags',
+                'vrm_services.context_processor.serve_context_processor',
             ],
         },
     },
